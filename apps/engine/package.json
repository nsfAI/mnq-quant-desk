{
  "name": "engine",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "backtest": "ts-node --transpile-only src/backtest.ts",
    "make-sample": "ts-node --transpile-only src/make_sample.ts"
  },
  "dependencies": {
    "express": "^4.18.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/ws": "^8.5.13",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}